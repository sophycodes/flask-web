# Dockerized Flask Application

This project demonstrates how to containerize a simple Flask web application using Docker. The application runs in an isolated Docker container, making it portable and easy to deploy across different environments.

![Screenshot](screenshot.png)
